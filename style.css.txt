:root {
    --bg-color: #f8fafc;
    --card-bg: #ffffff;
    --text-main: #1e293b;
    --text-muted: #64748b;
    --accent: #2563eb;
    --danger: #e11d48;
    --success: #10b981;
    --border: #e2e8f0;
    --radius: 16px;
    --shadow: 0 4px 6px -1px rgba(0,0,0,0.1);
}

body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg-color);
    color: var(--text-main);
    display: flex;
    justify-content: center;
    padding: 20px;
}

#app-container {
    width: 100%;
    max-width: 480px;
    margin-top: 20px;
}

.status-indicator {
    font-size: 0.75rem;
    color: var(--success);
    background: #ecfdf5;
    padding: 6px 14px;
    border-radius: 50px;
    font-weight: 600;
}

h1 { font-weight: 700; margin-top: 25px; letter-spacing: -0.02em; }

textarea {
    width: 100%;
    min-height: 160px;
    padding: 16px;
    border: 2px solid var(--border);
    border-radius: var(--radius);
    font-size: 1rem;
    resize: none;
    margin: 20px 0;
    transition: 0.3s;
}

textarea:focus { border-color: var(--accent); outline: none; }

.btn-primary {
    width: 100%;
    background: var(--accent);
    color: white;
    padding: 16px;
    border: none;
    border-radius: var(--radius);
    font-weight: 600;
    cursor: pointer;
    font-size: 1rem;
}

.card {
    background: white;
    padding: 20px;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
    margin-top: 15px;
}

.emergency-card { border-left: 6px solid var(--danger); }

.phone-number {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--danger);
    display: block;
    margin: 10px 0;
    text-decoration: none;
}

.hidden { display: none; }

/* Spinner Animado */
.loader {
    border: 3px solid #f3f3f3;
    border-top: 3px solid white;
    border-radius: 50%;
    width: 20px;
    height: 20px;
    animation: spin 1s linear infinite;
    display: inline-block;
    margin-right: 10px;
}

@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }